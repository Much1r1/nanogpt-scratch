# nanogpt-scratch
Decoder-only transformer built from scratch in PyTorch — attention, positional encoding, and training loop implemented without transformers/AutoModel, tested and benchmarked on a narrow corpus.
